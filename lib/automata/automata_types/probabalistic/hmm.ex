defmodule Automata.Types.HMM do
  @moduledoc """
  Implements Hidden Markov Model for global prediction.  Each agent
  is goal-oriented, i.e. associated with a distinct, high-level goal which it
  attempts to achieve.

  """

  # alias Automaton.Types.HMM.Config.Parser

  defmacro __using__(_opts) do
  end
end
